{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run LSP extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/client", "--folderPath=${workspaceFolder}/sample"
      ]
    }
  ]
}
